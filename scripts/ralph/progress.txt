# Ralph Progress Log
Started: 2026-05-11
---

## Codebase Patterns
- Project root: /Users/kimheejeong/Sites/mentor-platform
- CountUp component (src/components/CountUp.tsx) parses stat strings like "500+", "$2.3B" into prefix/num/suffix and animates with IntersectionObserver + requestAnimationFrame
- Quality check: run `npx tsc --noEmit` from project root
- Stack: React 18 + TypeScript + Vite + Tailwind CSS v3 + lucide-react
- Dark theme base colors: bg-[#0a0a0f] (page bg), bg-[#080810] (alt sections), bg-[#0e0e1a] (cards)
- useScrollAnimation hook (src/hooks/useScrollAnimation.ts): returns {ref, isVisible}. Initializes isVisible=true for prefers-reduced-motion. Observer disconnects after first trigger (once-only). Apply `animate-slide-up` when visible, `opacity-0` when not.
- tailwind.config.js has `animate-slide-up` (keyframe: opacity 0→1, translateY 24px→0, 0.6s ease-out forwards) and `animate-fade-in` already defined
- For stagger: pass `animationDelay` via inline style (CSS animation-delay); does NOT affect CSS transitions (hover effects unaffected)
- Primary brand: indigo-600 (#4f46e5), secondary: purple-600
- All text is Korean (한국어) - keep UI text in Korean
- Components live in src/components/, data in src/data/
- No routing - single page app, sections linked by anchor href="#sectionId"
- Mentor data: src/data/mentors.ts exports `mentors` array (20 items) and `categories` array
- Mentor type has: id, name, title, company, expertise[], bio, achievements[], availableFor[], avatarColor, initials, yearsExp, menteeCount, category
- Tailwind content paths configured for src/**/*.{ts,tsx}
- git identity: name=kimheejeong, email=hjkim.open@gmail.com
- git is initialized on branch `main`
- Dev server runs on http://localhost:5173 (may already be running)
- MentorModal: fixed inset-0 overlay with backdrop-blur, stopPropagation on panel click, ESC+body scroll lock via useEffect cleanup
- MentorCard receives `onOpenModal: () => void` prop; avatar is a `<button>`, name is a `<button>` for modal trigger
- MentorGrid owns `selectedMentor: Mentor | null` state; renders MentorModal before the section tag inside a Fragment

## 2026-05-11 - US-001
- Implemented 멘토 상세 모달 (MentorModal component)
- Files changed:
  - src/components/MentorModal.tsx (new) - full-screen modal with overlay, ESC key, body scroll lock, mentor details
  - src/components/MentorCard.tsx - added onOpenModal prop, made avatar and name into clickable buttons
  - src/components/MentorGrid.tsx - added selectedMentor state, imported Mentor type and MentorModal, wrapped return in Fragment
- **Learnings for future iterations:**
  - ESC key handler and body scroll lock both need useEffect cleanup (return function) to avoid leaks
  - stopPropagation on modal panel prevents backdrop click from closing when clicking inside
  - When converting div to button for interactivity, use `text-left` to preserve text alignment
  - Import `type { Mentor }` separately from `{ mentors, categories }` when you need the type in a new file
---

## 2026-05-11 - US-003
- Implemented 숫자 카운터 애니메이션 (Count-up animation for Stats section)
- Files changed:
  - src/components/CountUp.tsx (new) - parses stat value strings, uses IntersectionObserver to trigger once on viewport entry, animates with requestAnimationFrame + cubic easeOut over 1500ms
  - src/components/Stats.tsx - imported CountUp, replaced raw {stat.value} with <CountUp value={stat.value} />
- **Learnings for future iterations:**
  - Regex `/^([^0-9]*)([0-9]+(?:\.[0-9]+)?)(.*)$/` cleanly separates prefix/number/suffix from strings like "$2.3B", "500+", "4.9★"
  - `observer.disconnect()` inside the callback (after animation starts) ensures truly once-only behavior
  - Set `setCurrent(num)` at the end of the tick to guarantee final value is exact (avoids floating point drift)
  - The `started` ref guards against double-trigger if IntersectionObserver fires multiple times before disconnect
---

## 2026-05-11 - US-004
- Implemented FAQ 아코디언 섹션 (8 Korean FAQ items for startup founders)
- Files changed:
  - src/components/FAQ.tsx (new) - accordion FAQ with exclusive open behavior, ChevronDown rotation, dark theme
  - src/App.tsx - imported and inserted FAQ between Stats and Contact
  - src/components/Navbar.tsx - added FAQ link (href=#faq) to navLinks array
- **Learnings for future iterations:**
  - Accordion exclusive behavior uses `openIndex === i ? null : i` toggle pattern with a single `openIndex: number | null` state
  - max-h-0 / max-h-96 with `transition-all duration-300` gives smooth height animation without JS measurement
  - ChevronDown rotation: `rotate-180` class toggled via conditional className string
  - FAQ section uses bg-[#0a0a0f] (same as page bg) to alternate with Contact's bg-[#080810]
---

## 2026-05-11 - US-002
- Implemented How It Works 섹션 between Hero and MentorGrid
- Files changed:
  - src/components/HowItWorks.tsx (new) - 3-step process section with icons, numbered badges, desktop arrow connectors
  - src/App.tsx - imported and inserted HowItWorks between Hero and MentorGrid
- **Learnings for future iterations:**
  - Section bg-[#080810] alternates with bg-[#0a0a0f] for visual rhythm between sections
  - Arrow connectors on desktop use `absolute right-0 translate-x-1/2` to center between adjacent step blocks
  - lucide-react icons used: FileText (신청서), UserCheck (매칭), Video (세션), ArrowRight (connector + CTA)
---

## 2026-05-11 - US-005
- Implemented 스크롤 진입 애니메이션 (scroll entry fade-in + slide-up animation)
- Files changed:
  - src/hooks/useScrollAnimation.ts (new) - custom hook with IntersectionObserver, once-only, prefers-reduced-motion aware
  - src/components/MentorCard.tsx - added delay prop, uses hook, animates with animate-slide-up + animationDelay
  - src/components/MentorGrid.tsx - passes delay={index * 50} for stagger
  - src/components/Program.tsx - section header animated
  - src/components/Stats.tsx - section header animated
  - src/components/FAQ.tsx - section header animated
  - src/components/Contact.tsx - section header animated
- **Learnings for future iterations:**
  - tailwind.config.js already had animate-slide-up defined (opacity 0→1, translateY 24px→0, forwards fill)
  - CSS animation-delay (inline style animationDelay) only applies to CSS animations, not CSS transitions - hover effects are unaffected
  - Initialize isVisible via useState lazy initializer to avoid flash for prefers-reduced-motion users
  - Pattern: className={isVisible ? 'animate-slide-up' : 'opacity-0'} for clean toggle
---

## 2026-05-11 - US-006
- Implemented 모바일 하단 고정 CTA 바 (mobile bottom sticky CTA bar)
- Files changed:
  - src/components/MobileCtaBar.tsx (new) - fixed bottom bar, md:hidden, scroll-triggered visibility, Contact IntersectionObserver hide
  - src/App.tsx - imported and added MobileCtaBar after Footer
- **Learnings for future iterations:**
  - Use `useRef` for scroll/intersection state inside useEffect closures to avoid stale closure issues (refs are always current)
  - `translate-y-full` / `translate-y-0` with `transition-transform duration-300` gives smooth slide-up-from-bottom animation
  - `style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}` handles iOS notch/home-indicator safe area
  - Hero section has `min-h-screen`; threshold of `window.innerHeight * 0.8` reliably detects "past hero"
  - `md:hidden` keeps the bar desktop-invisible without JS
---

## 2026-05-11 - US-007
- Implemented 멘토 검색 텍스트 하이라이트 (search text highlighting in mentor cards)
- Files changed:
  - src/components/MentorCard.tsx - added `searchQuery?: string` prop and `highlightText` helper function; applied to name, title, and expertise tags
  - src/components/MentorGrid.tsx - passed `searchQuery={search}` to each MentorCard
- **Learnings for future iterations:**
  - `text.split(new RegExp(`(${escaped})`, 'gi'))` with a capturing group includes matched substrings in the result array at odd indices (i % 2 === 1), making it easy to identify and wrap matches
  - Escape regex special characters with `.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')` before building dynamic RegExp
  - Returning `<>...</>` from a render helper function (React.ReactNode) avoids key conflicts and works cleanly with TypeScript — no need to import React explicitly in TSX files
  - Existing `search` state in MentorGrid can be passed directly as `searchQuery` — no new state needed
---
